# Test

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - environments/test/monitoring/**
      - modules/monitoring/**
      - pipelines/test/terraform-test-monitoring.yml
      - pipelines/templates/**

pr:
  autoCancel: true
  branches:
    include:
      - main
  paths:
    include:
      - environments/test/monitoring/**
      - modules/monitoring/**
      - pipelines/test/terraform-test-monitoring.yml
      - pipelines/templates/**

pool:
  name: MultiOsPool
  demands:
    - Agent.Name -equals prod-win-vm-age

stages:
  - template: ../templates/terraform-domain.yml
    parameters:
      environmentName: test
      domainName: monitoring
      terraformWorkingDirectory: environments/test/monitoring
      serviceConnection: sc-ealz-test
      backendResourceGroup: rg-ealz-tfstate-eus2-001
      backendStorageAccount: stealztfstate001
      backendContainer: tfstate
      backendKey: test-monitoring.tfstate
      deploymentEnvironment: test