# Test

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - environments/test/identity/**
      - modules/identity/**
      - pipelines/test/terraform-test-identity.yml
      - pipelines/templates/**

pr:
  autoCancel: true
  branches:
    include:
      - main
  paths:
    include:
      - environments/test/identity/**
      - modules/identity/**
      - pipelines/test/terraform-test-identity.yml
      - pipelines/templates/**

pool:
  name: MultiOsPool
  demands:
    - Agent.Name -equals prod-win-vm-age

stages:
  - template: ../templates/terraform-domain.yml
    parameters:
      environmentName: test
      domainName: identity
      terraformWorkingDirectory: environments/test/identity
      serviceConnection: sc-ealz-dev
      backendResourceGroup: rg-ealz-tfstate-eus2-001
      backendStorageAccount: stealztfstate001
      backendContainer: tfstate
      backendKey: test-identity.tfstate
      deploymentEnvironment: test