# Test

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - environments/test/governance/**
      - modules/governance/**
      - pipelines/test/terraform-test-governance.yml
      - pipelines/templates/**

pr:
  autoCancel: true
  branches:
    include:
      - main
  paths:
    include:
      - environments/test/governance/**
      - modules/governance/**
      - pipelines/test/terraform-test-governance.yml
      - pipelines/templates/**

pool:
  name: MultiOsPool
  demands:
    - Agent.Name -equals prod-win-vm-age

stages:
  - template: ../templates/terraform-domain.yml
    parameters:
      environmentName: test
      domainName: governance
      terraformWorkingDirectory: environments/test/governance
      serviceConnection: sc-ealz-test
      backendResourceGroup: rg-ealz-tfstate-eus2-001
      backendStorageAccount: stealztfstate001
      backendContainer: tfstate
      backendKey: test-governance.tfstate
      deploymentEnvironment: test