trigger: none

pr:
  autoCancel: true
  branches:
    include:
      - main
  paths:
    include:
      - environments/dev/**
      - modules/**
      - shared/**
      - pipelines/terraform-pr-validation.yml
      - pipelines/terraform-dev.yml

pool:
  name: MultiOsPool
  demands:
    - Agent.Name -equals prod-win-vm-age

variables:
  terraformVersion: '1.15.9'
  terraformWorkingDirectory: 'environments/dev'
  serviceConnection: 'sc-ealz-dev'

  backendResourceGroup: 'rg-ealz-tfstate-eus2-001'
  backendStorageAccount: 'stealztfstate001'
  backendContainer: 'tfstate'
  backendKey: 'dev.tfstate'

stages:
  - stage: Validate
    displayName: Terraform Pull Request Validation

    jobs:
      - job: TerraformValidation
        displayName: Terraform Validate and Plan

        steps:
          - checkout: self

          - task: TerraformInstaller@1
            displayName: Install Terraform
            inputs:
              terraformVersion: '$(terraformVersion)'

          - powershell: |
              terraform fmt -check -recursive -diff
            displayName: Terraform Format Check
            workingDirectory: '$(Build.SourcesDirectory)'

          - task: TerraformTaskV4@4
            displayName: Terraform Init
            inputs:
              provider: 'azurerm'
              command: 'init'
              workingDirectory: '$(terraformWorkingDirectory)'
              backendServiceArm: '$(serviceConnection)'
              backendAzureRmUseEntraIdForAuthentication: true
              backendAzureRmResourceGroupName: '$(backendResourceGroup)'
              backendAzureRmStorageAccountName: '$(backendStorageAccount)'
              backendAzureRmContainerName: '$(backendContainer)'
              backendAzureRmKey: '$(backendKey)'
              commandOptions: '-input=false'

          - task: TerraformTaskV4@4
            displayName: Terraform Validate
            inputs:
              provider: 'azurerm'
              command: 'validate'
              workingDirectory: '$(terraformWorkingDirectory)'

          - task: TerraformTaskV4@4
            displayName: Terraform Plan
            inputs:
              provider: 'azurerm'
              command: 'plan'
              workingDirectory: '$(terraformWorkingDirectory)'
              environmentServiceNameAzureRM: '$(serviceConnection)'
              commandOptions: '-out=tfplan -input=false'

          - powershell: |
              terraform show -no-color tfplan | Out-File -FilePath tfplan.txt -Encoding utf8
            displayName: Generate Readable Terraform Plan
            workingDirectory: '$(terraformWorkingDirectory)'

          - task: PublishPipelineArtifact@1
            displayName: Publish Terraform Plan
            inputs:
              targetPath: '$(terraformWorkingDirectory)/tfplan'
              artifact: 'terraform-pr-plan'
              publishLocation: 'pipeline'

          - task: PublishPipelineArtifact@1
            displayName: Publish Readable Terraform Plan
            inputs:
              targetPath: '$(terraformWorkingDirectory)/tfplan.txt'
              artifact: 'terraform-pr-plan-readable'
              publishLocation: 'pipeline'