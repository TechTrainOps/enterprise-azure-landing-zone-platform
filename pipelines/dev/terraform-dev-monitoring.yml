trigger:
  branches:
    include:
      - main
  paths:
    include:
      - environments/dev/monitoring/**
      - modules/monitoring/**
      - pipelines/dev/terraform-dev-monitoring.yml
      - pipelines/templates/**

pr:
  autoCancel: true
  branches:
    include:
      - main
  paths:
    include:
      - environments/dev/monitoring/**
      - modules/monitoring/**
      - pipelines/dev/terraform-dev-monitoring.yml
      - pipelines/templates/**

pool:
  name: MultiOsPool
  demands:
    - Agent.Name -equals prod-win-vm-age

stages:
  - template: ../templates/terraform-domain.yml
    parameters:
      environmentName: dev
      domainName: monitoring
      terraformWorkingDirectory: environments/dev/monitoring
      serviceConnection: sc-ealz-dev
      backendResourceGroup: rg-ealz-tfstate-eus2-001
      backendStorageAccount: stealztfstate001
      backendContainer: tfstate
      backendKey: dev-monitoring.tfstate
      deploymentEnvironment: dev