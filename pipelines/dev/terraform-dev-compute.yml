trigger:
  branches:
    include:
      - main
  paths:
    include:
      - environments/dev/compute/**
      - modules/compute/**
      - pipelines/dev/terraform-dev-compute.yml
      - pipelines/templates/**

pr:
  autoCancel: true
  branches:
    include:
      - main
  paths:
    include:
      - environments/dev/compute/**
      - modules/compute/**
      - pipelines/dev/terraform-dev-compute.yml
      - pipelines/templates/**

pool:
  name: MultiOsPool
  demands:
    - Agent.Name -equals prod-win-vm-age

stages:
  - template: ../templates/terraform-domain.yml
    parameters:
      environmentName: dev
      domainName: compute
      terraformWorkingDirectory: environments/dev/compute
      modulePath: modules/compute
      serviceConnection: sc-ealz-dev
      backendResourceGroup: rg-ealz-tfstate-eus2-001
      backendStorageAccount: stealztfstate001
      backendContainer: tfstate
      backendKey: dev-compute.tfstate
      deploymentEnvironment: dev