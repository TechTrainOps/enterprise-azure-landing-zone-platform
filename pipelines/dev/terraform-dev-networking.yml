trigger:
  branches:
    include:
      - main
  paths:
    include:
      - environments/dev/networking/**
      - modules/networking/**
      - pipelines/dev/terraform-dev-networking.yml
      - pipelines/templates/**

pr:
  autoCancel: true
  branches:
    include:
      - main
  paths:
    include:
      - environments/dev/networking/**
      - modules/networking/**
      - pipelines/dev/terraform-dev-networking.yml
      - pipelines/templates/**

pool:
  name: MultiOsPool
  demands:
    - Agent.Name -equals prod-win-vm-age

stages:
  - template: ../templates/terraform-domain.yml
    parameters:
      environmentName: dev
      domainName: networking
      terraformWorkingDirectory: environments/dev/networking
      serviceConnection: sc-ealz-dev
      backendResourceGroup: rg-ealz-tfstate-eus2-001
      backendStorageAccount: stealztfstate001
      backendContainer: tfstate
      backendKey: dev-networking.tfstate
      deploymentEnvironment: dev