trigger:
  branches:
    include:
      - main
  paths:
    include:
      - environments/prod/compute/**
      - modules/compute/**
      - pipelines/prod/terraform-prod-compute.yml
      - pipelines/templates/**

pr:
  autoCancel: true
  branches:
    include:
      - main
  paths:
    include:
      - environments/prod/compute/**
      - modules/compute/**
      - pipelines/prod/terraform-prod-compute.yml
      - pipelines/templates/**

pool:
  name: MultiOsPool
  demands:
    - Agent.Name -equals prod-win-vm-age

stages:
  - template: ../templates/terraform-domain.yml
    parameters:
      environmentName: prod
      domainName: compute
      terraformWorkingDirectory: environments/prod/compute
      modulePath: modules/compute
      serviceConnection: sc-ealz-prod
      backendResourceGroup: rg-ealz-tfstate-eus2-001
      backendStorageAccount: stealztfstate001
      backendContainer: tfstate
      backendKey: prod-compute.tfstate
      deploymentEnvironment: prod