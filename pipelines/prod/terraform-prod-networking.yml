trigger:
  branches:
    include:
      - main
  paths:
    include:
      - environments/prod/networking/**
      - modules/networking/**
      - pipelines/prod/terraform-prod-networking.yml
      - pipelines/templates/**

pr:
  autoCancel: true
  branches:
    include:
      - main
  paths:
    include:
      - environments/prod/networking/**
      - modules/networking/**
      - pipelines/prod/terraform-prod-networking.yml
      - pipelines/templates/**

pool:
  name: MultiOsPool
  demands:
    - Agent.Name -equals prod-win-vm-age

stages:
  - template: ../templates/terraform-domain.yml
    parameters:
      environmentName: prod
      domainName: networking
      terraformWorkingDirectory: environments/prod/networking
      serviceConnection: sc-ealz-prod
      backendResourceGroup: rg-ealz-tfstate-eus2-001
      backendStorageAccount: stealztfstate001
      backendContainer: tfstate
      backendKey: prod-networking.tfstate
      deploymentEnvironment: prod