# Prod

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - environments/prod/monitoring/**
      - modules/monitoring/**
      - pipelines/prod/terraform-prod-monitoring.yml
      - pipelines/templates/**

pr:
  autoCancel: true
  branches:
    include:
      - main
  paths:
    include:
      - environments/prod/monitoring/**
      - modules/monitoring/**
      - pipelines/prod/terraform-prod-monitoring.yml
      - pipelines/templates/**

pool:
  name: MultiOsPool
  demands:
    - Agent.Name -equals prod-win-vm-age

stages:
  - template: ../templates/terraform-domain.yml
    parameters:
      environmentName: prod
      domainName: monitoring
      terraformWorkingDirectory: environments/prod/monitoring
      serviceConnection: sc-ealz-prod
      backendResourceGroup: rg-ealz-tfstate-eus2-001
      backendStorageAccount: stealztfstate001
      backendContainer: tfstate
      backendKey: prod-monitoring.tfstate
      deploymentEnvironment: prod